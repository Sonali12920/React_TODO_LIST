import React from "react";

function About() {
  return (
    <React.Fragment>
      <p style={{ textAlign: "center", padding: "30px;" }}>
        This is Todo List made using React. Todo List contain all of the tasks
        that we need to complete on a given day which is used for managing time
        that enables us to lay out everything that we need to plan and
        prioritize our day from there. It motivates us to keep moving forward.
      </p>
    </React.Fragment>
  );
}

export default About;
